# Milestone 02 — Understanding and discovery

Improve structured task understanding and local capability discovery. Keep the deterministic analyzer
as a fallback. Add a replaceable structured-output analyzer only behind the interface. Discovery must
validate manifests, skip symlinks, report errors, avoid executing candidates, and load minimal context.
