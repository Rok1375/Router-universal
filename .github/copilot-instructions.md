Follow `AGENTS.md` and the project constitution. Keep the core framework-agnostic and local-first.
Every capability must be represented by a validated manifest and accessed through an adapter.
Every write must have tests and verification evidence. Permission decisions are policy outputs, not
assumptions. Update `docs/project-state.md` when a milestone changes status.
